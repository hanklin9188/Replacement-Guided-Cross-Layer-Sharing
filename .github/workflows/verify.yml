name: Verify research artifact

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  public-integrity:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Recompute public claims and audit repository hygiene
        run: python scripts/verify_repository.py
