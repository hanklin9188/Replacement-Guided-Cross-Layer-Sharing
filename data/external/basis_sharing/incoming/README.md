# Basis Sharing incoming payload

Copy `predictions.csv`, `byte_manifest.csv`, `quantization.csv`, and
`run_manifest.json` here. The files are intentionally ignored until the
scheduled validator accepts them. See `../../README.md` and `../../schema/`.
